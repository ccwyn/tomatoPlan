<!--pages/logs/logs.wxml--><text>pages/logs/logs.wxml</text>ainer" scrollY="true">
        <view class="log panel">
            <view class="log_item" wx:for="{{logs}}" wx:for-index="$index" wx:for-item="log" wx:key="*this">
                <text class="log_start">{{log.startTime}}</text>
                <text class="log_action">{{log.action}}</text>
                <text class="log_action">{{log.name}}</text>
            </view>
        </view>
    </scroll-view>
    <view class="clear">
        <button bindtap="switchModal" class="clear_btn" size="mini">清除记录</button>
    </view>
    <modal bindcancel="switchModal" bindconfirm="clearLog" cancelText="取消" confirmText="确定" hidden="{{modalHidden}}" title="提示">
	    是否清除记录？此操作不可恢复!
	</modal>
</block>
<view class="nodata" wx:else>
    <image class="nodata_img" src="../../image/nodata.png"></image>
</view>
<toast bindchange="hideToast" hidden="{{toastHidden}}">清除成功！</toast>
